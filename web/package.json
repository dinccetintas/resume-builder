{
  "name": "job-application-pipeline-web",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js app for the automated job-application pipeline (UI + API + pipeline libs).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
